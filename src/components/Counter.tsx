import React from 'react';
import './Counter.scss';

const Counter = () => {
    return (
        <div>
            <button className='button'>increment</button>
        </div>
    );
};

export default Counter;